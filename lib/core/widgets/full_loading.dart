import 'package:flutter/material.dart';

class FullLoading extends StatelessWidget {
  const FullLoading({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Loading...");
  }
}
